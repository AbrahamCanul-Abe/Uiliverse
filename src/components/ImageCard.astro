---
interface Props {
	alt : string;
  src : string;
  price : string;
}

const { alt, src, price } = Astro.props;
---

  <img
    alt={ alt }
    src={ src }
    class="h-full w-full object-cover rounded-br-3xl rounded-tl-3xl"
  />
  <span class=" whitespace-nowrap rounded-full bg-purple-100 px-2.5 py-0.5 text-sm text-purple-700 absolute top-0 right-0">
    $ { price }
  </span>

